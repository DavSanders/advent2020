import { getInputArray } from "../getInputArray";

const daydata = getInputArray();

const getFirstSolution = (daydata: string[]) => {};

const one = getFirstSolution(daydata);
console.log(one);

const getSecondSolution = (daydata: string[]) => {};

const two = getSecondSolution(daydata);
console.log(two);
